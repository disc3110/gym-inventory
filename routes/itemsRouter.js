/*
	•	GET /items → list all items
	•	GET /items/new → form (with category dropdown)
	•	POST /items → create
	•	GET /items/:id → item detail (show updated_at, etc.)
	•	GET /items/:id/edit → edit form
	•	POST /items/:id/delete → delete

*/